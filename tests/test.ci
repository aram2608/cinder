mod main;
import math;

extern printf(str fmt, ...) -> int32

def get(int32 a, int32 b) -> int32
    return a + b;
end

def main() -> int32
    int32: b = get(1, 4);
    int32: a = 4;
    bool: flag = 1 > 4;
    str: test = "hello world";

    if 2 == 2
        printf("%d\n", 1);
    else
        printf("%d\n", 0);
    end

    for int32: it = 0; it < 4; ++it
        printf("Hello world! %d\n", 4);
    end

    int32: val = 0;
    while val < 4
        printf("Hello %d\n", val);
        ++val;
    end

    int32: x = add(2, 3);
    printf("X from imported math %d\n", x);
    return 0;
end
